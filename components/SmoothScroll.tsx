export default function SmoothScroll() {
  // Native CSS smooth scroll is applied on <html>. Lenis intentionally omitted
  // because its transform-based scroll conflicts with position:fixed elements.
  return null;
}
